# Frontend Environment Variables

# API Base URL (required for API connectivity)
# Development:
VITE_PICK_API=http://localhost:8080

# Production (update with your actual API domain):
# VITE_PICK_API=https://api.yourdomain.com

# Optional: Enable development features
VITE_DEV_MODE=true

# Note: All VITE_ prefixed variables are exposed to the browser
# Do NOT put sensitive secrets here - use server-side env vars instead
